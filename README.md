# 🧾 Student Records Using Files (C)

A simple yet functional **C program** that stores, reads, and displays student records using **file handling**.  
This project demonstrates the use of `struct`, `FILE` operations, and clean formatted I/O — ideal for beginners learning persistent data storage in C.

---

## 📘 Features

- Add new student details (Name, Roll Number, Marks)
- Automatically stores data in a `.txt` file (`student.txt`)
- Reads and displays all stored records
- Preserves previous data using append mode
- Simple and clean output format
- Error handling for file operations

---
